# mini-agent-demo

A tiny demo showing a dice-rolling function.

- `dice.js` exports `roll(sides)` which returns a random integer between 1 and `sides` (inclusive).
- `dice.test.js` calls `roll(6)` five times and prints the results to the console.
